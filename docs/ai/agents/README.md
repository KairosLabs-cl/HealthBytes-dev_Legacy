# AI Agents

Executable agents are registered in `.agents/router.json`.

Current executable agents:

- `backend-dev`
- `frontend-dev`
- `healthbytes-qa`
- `code-reviewer`
- `test-runner`
- `hermes-verifier`

Use this folder for human-readable operating guidance and ownership rules. Keep executable profiles in `.agents/agents/`.

