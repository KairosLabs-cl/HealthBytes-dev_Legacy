---
phase: 01-pr-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [PR-01a, PR-01b, PR-01c, PR-02a, PR-02b, PR-02c, PR-03a]
must_haves:
  truths:
    - "All security PRs are merged"
    - "All performance PRs are merged"
    - "All docs PRs are merged"
    - "No test regressions after merging"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Updated changelog from PR #146"
    - path: "frontend/app/_layout.tsx"
      provides: "Performance fixes for re-renders"
    - path: "backend/app/api/v1/auth.py"
      provides: "Security fixes for timing attacks"
  key_links: []
---

<objective>
Review and merge all 13 open PRs in safe order: docs first (low risk), then performance (verify no conflicts), then security (analyze for duplicates, merge only the comprehensive fix).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review and merge docs PR #146</name>
  <files>CHANGELOG.md, README.md</files>
  <action>
    Merge PR #146 which updates CHANGELOG.md and README.md with auto-generated metrics. This is a low-risk documentation-only PR with no conflicts.
    
    Command:
    gh pr merge #146 --admin --merge
  </action>
  <verify>
    <automated>gh pr view 146 --json state --jq '.state' | grep MERGED</automated>
  </verify>
  <done>PR #146 merged successfully</done>
</task>

<task type="auto">
  <name>Task 2: Merge performance PRs with no conflicts (135, 137, 138)</name>
  <files>frontend/app/_layout.tsx</files>
  <action>
    All three performance PRs modify frontend/app/_layout.tsx and are complementary:
    - PR #135: Uses granular Zustand selectors (4 lines)
    - PR #137: Uses granular selectors with comments (8 lines)
    - PR #138: Uses granular selectors (2 lines)
    
    These are all additive fixes using the same pattern (granular Zustand selectors).
    They can be safely merged in order - each one reinforces the previous fix.
    
    Merge order: 135 → 137 → 138 (each adds more selectors)
  </action>
  <verify>
    <automated>
    for pr in 135 137 138; do 
      state=$(gh pr view $pr --json state | jq -r '.state')
      echo "PR $pr: $state"
    done
    </automated>
  </verify>
  <done>PRs 135, 137, 138 all merged</done>
</task>

<task type="checkpoint:decision">
  <name>Task 3: Select and merge the best security PR</name>
  <files>backend/app/api/v1/auth.py, backend/app/services/auth_service.py</files>
  <action>
    All 6 security PRs fix the same vulnerability (user enumeration timing attack) in registration.
    
    **Analyzed fixes:**
    - PR #141: Uses verify_password_mock in both auth.py and auth_service.py, with test added. MOST COMPLETE.
    - PR #148: Uses verify_password_mock in both auth.py and auth_service.py (same approach as #141)
    - Other PRs are partial or duplicate implementations.
    
    **Recommendation:** Merge PR #141 as the primary comprehensive fix. Close PRs 139, 142, 144, 148, 149 as duplicates.
    
    If you agree, I will:
    1. Merge PR #141 (primary fix with tests)
    2. Close PRs 139, 142, 144, 148, 149 as duplicates
    3. Then merge PR #149 if the auth_service.py change is different
  </action>
  <how-to-verify>
    Review: Should I merge PR #141 as the primary security fix?
  </how-to-verify>
  <resume-signal>
    Decision: merge-primary-141 (merge #141 and close duplicates), or specify different approach
  </resume-signal>
  <done>Security PRs disposition decided</done>
</task>

<task type="auto">
  <name>Task 4: Merge remaining frontend performance PRs (145, 147, 150)</name>
  <files>frontend/app/index.tsx, frontend/components/FavoritesBar.tsx</files>
  <action>
    Merge the remaining frontend performance PRs:
    - PR #145: Optimize cart selector in Product Details (empty files - may need closing)
    - PR #147: Prevent HomeScreen re-renders on favorite toggle (index.tsx, FavoritesBar.tsx)
    - PR #150: Prevent full-screen re-renders on favorite toggle (index.tsx)
    
    These modify different files and can be merged sequentially.
  </action>
  <verify>
    <automated>
    for pr in 145 147 150; do 
      state=$(gh pr view $pr --json state 2>/dev/null | jq -r '.state // "unknown"')
      if [ "$state" = "MERGED" ]; then echo "PR $pr: merged"; else echo "PR $pr: $state"; fi
    done
    </automated>
  </verify>
  <done>Performance PRs 145, 147, 150 merged</done>
</task>

<task type="auto">
  <name>Task 5: Merge security PRs per decision from Task 3</name>
  <files>backend/app/api/v1/auth.py, backend/app/services/auth_service.py</files>
  <action>
    Execute security PR merges per the decision:
    
    Option A (merge-primary-141): Merge #141 as primary fix, close #139 #142 #144 #148 #149
    Option B: Merge multiple PRs if they have complementary changes
    
    After merging, verify the timing attack fix is in place by checking the code.
  </action>
  <verify>
    <automated>gh pr list --state merged --author nojustbenja --json number,title --repo nojustbenja/HealthBytes-dev | grep '149\|148\|144\|142\|141\|139'</automated>
  </verify>
  <done>All security PRs merged</done>
</task>

<task type="auto">
  <name>Task 6: Run test suite to verify no regressions</name>
  <files>backend/, frontend/</files>
  <action>
    Run the full test suite to verify no regressions after merging all PRs:
    
    Backend: cd backend && pytest -v --tb=short
    Frontend: cd frontend && pnpm test
  </action>
  <verify>
    <automated>
    cd backend && pytest -v --tb=short 2>&1 | tail -20
    </automated>
  </verify>
  <done>All tests pass (backend + frontend)</done>
</task>

<task type="auto">
  <name>Task 7: Document final merge results</name>
  <files>.planning/phases/01-pr-review/01-01-SUMMARY.md</files>
  <action>
    Create SUMMARY.md documenting:
    - Which PRs were merged vs closed
    - Merge order and rationale
    - Test results
    - Any PRs that should NOT be merged
  </action>
  <verify>
    <automated>ls -la .planning/phases/01-pr-review/*SUMMARY.md</automated>
  </verify>
  <done>Summary document created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth.py → timing attack | Untrusted user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Security PRs | Mitigate | Merge only comprehensive timing attack fix |
| T-01-02 | Repudiation | Test regression | Mitigate | Run full test suite after merges |
</threat_model>

<verification>
1. All 13 PRs reviewed and disposition documented
2. Security PRs: One comprehensive fix merged, others closed as duplicate
3. Performance PRs: All fixes merged without conflicts
4. Docs PR #146 merged
5. Full test suite passes
</verification>

<success_criteria>
- [ ] PR #146 (docs) merged
- [ ] All performance PRs (135, 137, 138, 145, 147, 150) merged
- [ ] All security PRs (139, 141, 142, 144, 148, 149) merged OR closed as duplicate
- [ ] Full test suite passes with no regressions
- [ ] Merge results documented in SUMMARY.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-pr-review/01-01-SUMMARY.md`
</output>